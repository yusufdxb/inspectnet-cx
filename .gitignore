.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.py[cod]
*.egg-info/
dist/
build/
*.onnx
datasets/
artifacts/
reports/*.json
reports/*.md
!reports/README.md

# Training artifacts: anomalib / lightning checkpoints, scratch run trees.
# These are reproducible from the documented training commands and exceed
# GitHub's 100 MB file size limit.
reports/**/*.ckpt
reports/verification/
reports/tmp_padim_probe2/

# Canonical HF model-card clone lives here but is its own git repo.
huggingface/inspectnet-cx/
